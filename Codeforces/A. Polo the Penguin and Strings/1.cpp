// link-https://codeforces.com/problemset/problem/288/A

/* #include<bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    long long n,i,c=0,f=0;
    int k;
    cin>>n>>k;
    if(k>n)
    {
        cout<<"-1";
        return 0;
    }
    while(c<=n)
    {
         for(i=0;i<k;i++)
         {
            cout<<char(i+97);
            c++;
            if(c>=n)
            {
                f=1;
                break;
            }
         }
         if(f)
         {
            break;
         }
    }


  return 0;
} */
